import { Box, styled } from "@mui/material";

export const ListsWrapper = styled(Box)({
  padding: "20px",
  display: "flex",
  gap: "10px",
});

export const ListBox = styled(Box)({
  flex: 1,
});

export const LanguageTextWrapper = styled(Box)({
  marginTop: "10px",
  marginBottom: "20px",
  display: "flex",
  flexDirection: "column",
  gap: "10px",
});

export const ButtonBox = styled(Box)({
  display: "flex",
  gap: "10px",
});
