import { Box, styled } from "@mui/material";

export const ModalWrapperBox = styled(Box)({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  gap: "15px",
});

export const RowBox = styled(Box)({
  display: "flex",
  flexDirection: "row",
  justifyContent: "center",
  gap: "15px",
});
